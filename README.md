# Punisher.VPPTool
Extract and create .VPP archives.

## Usage

`VPPTool <mode> <input>`

`<mode>` - (e)xtract or (c)reate

Examples:

To extract:
`VPPTool e file.vpp`

To create:
`VPPTool c foldername`
